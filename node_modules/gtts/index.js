'use strict';
class gTTS {
  constructor(text, lang) { this.text = text; this.lang = lang || 'en'; }
  save(path, callback) { callback(new Error('TTS unavailable: gtts not installed due to security policy')); }
  stream() { const { Readable } = require('stream'); const r = new Readable(); r.push(null); return r; }
}
module.exports = gTTS;
